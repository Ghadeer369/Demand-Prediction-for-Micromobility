# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Virtual environment
.venv/
venv/

# Data (never commit raw/processed data or trained models to git)
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep
models/*
!models/.gitkeep

# Outputs — figures and prediction CSVs ARE versioned (small, referenced by
# the README, not sensitive) — only raw/processed data and models are not.

# OS
.DS_Store
